[agentx]
ollama_host = "localhost:11434"
ollama_model = "gpt-oss"
ollama_initial_load_timeout_seconds = 120
screen_side = "left"

[agentix]
# Enable Agentix integration for code analysis tools
# Requires: libcst, agentix packages installed
# If disabled, basic tool execution still works via Ollama
enabled = false
host = "localhost:8000"
classify_prompts = false
debug = false
available_tools = ["cst", "ast"]
