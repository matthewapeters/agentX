1. [DONE] agentx should accept a --session value on launch to define what the session name is.  By default, if not present, current session name is generated by the application.  This allows the user to create multiplex layouts with commands (IE zellij) and open multiple surfaces with predictable session names.
2. [DONE] agent response widget in the output pannel should scroll veritically with the incoming text so the user can read it
3. [DONE] classification widget in the output panel could be reduced to a single line (no box border) as it always only contains one line of information.  Retain emoji and title, just move the classification to the same line.
4. create a separate interface to ollama for small, parallel prompts (classification, triplet / wm value extractions.  Leverage channels.
5. look at how neo4j and chromadb (containerized and local) can be used to facilitate memory, detail recall, context management.  Goal is to reduce conversational text volume in context.  Roles these can play in tools/results management?
6. [DONE — assistant bodies render markdown as ANSI. Streaming: always the per-line scanner (tier 1 inline **bold**/`code`/#-### headers, tier 2 unordered/ordered lists + blockquotes). Finalized answers: the "native" renderer (default) adds GFM tables via lipgloss/table with row borders + zebra striping, and chroma syntax-highlighted code blocks. `[agentx.output] markdown_renderer` = "native" (default) or "scanner". Glamour was trialed then retired in favor of native (clearer tables, smaller dep footprint) — see ADR 0007] feature: md -> bubbletea enahanced rendering: in the output panel, detect the use of markdown enhancements IE `I am **AgentX**, your local agent` -> render `**AgentX**` as ansi-escape colored text.  If feature already exists in bubbletea or bubble, that is great.  If not, we will need a theming mechanism to treat headers of various levels distinctly.  Table rendering would be a HUGE bonus.  Some things like italics cannot be handled, and that is OK, but leveraging charm.land's ability to beuatify the TUI experience is highly desireable.
7. when an output widget scrolls to or past the top of the screen, it should collapse, conserving space.  The agent response widget could possibly be a percentage of the available output real-estate to minimize the need for the user to navigate and scroll to read responses.  The user and thinking widgets definitely should adhere to configured max size.
8. Tasks objects will implement Remember() which will add the command and results to WM.  The prompt for the task planner will be updated to include language explaining that a task has an optional "remember" bool that will keep the task results in WM in the event that the task is likely to satisfy the following: a) will likely be referred to again and b) is unlikely to change much.  An "age" property will be computed when the WM entry is added to the context (which means the WM element must have a generated timestamp).  This will provide the LLM the ability to decide if the information is out-of-date.
9. Tasks will implement Forget(), which will remove their WM entries.  This will be invoked by through a tool offered to the LLM, and by an internal process that applies TTL logic to WM entries that have been Remember()ed
10.  [TODO] Replace hand‑rolled flag parser with a standard library (cobra/viper) to get help, usage and consistent error handling.
11.  [TODO] add tests covering config-package edge cases (missing required flags, malformed JSON path for runtime field) right now internal/config is untested while most other packages have at least one test file adding a small config_test.go would catch regressions before they ship location: /Projects/agentX/internal/config
12.  [TODO] move addSurfaces to a shared location so it can serve both tests/surfaces and the main codebase for multi-process registries (local + external server context) this avoids duplicate registry logic when surfaces are invoked from multiple contexts location: /Projects/agentX/internal/surfaces
13. The user approval process needs a make-over: it should operate out of the input widget: change the border color to draw attention to the prompt - print the prompt in the border; print the options in the first line of the input widget and capture the user's selection - do not do a key capture: allow the user to type their response and hit enter to submit.  After capturing, the output should show the request and the user's selection (not the letter, but the corresponding option text) as a one-line, gray event log (does not need to be captured as part of the context).  The user interface should have consistent behavior, regardless of what the system is seeking user permission for.


